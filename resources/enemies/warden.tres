[gd_resource type="Resource" script_class="EnemyData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/enemy_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "warden"
name = "Warden"
hp_mult = 1.05
dmg_mult = 0.95
count_min = 1
count_max = 1
pattern = PackedInt32Array(0, 3, 0, 4)
debuff_stacks = 1
block_amount = 12
strength_gain = 2
rule_trigger = PackedInt32Array(1)
rule_threshold = PackedInt32Array(20)
rule_action = PackedInt32Array(5)
rule_value = PackedInt32Array(0)
rule_once = PackedInt32Array(0)
