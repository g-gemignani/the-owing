[gd_resource type="Resource" script_class="EventData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/event_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "dead_merchant"
title = "The Dead Merchant"
description = "His pack is intact. He is not."
choice_labels = PackedStringArray("Take the coin (+90 gold)", "Take the goods (gain a card)", "Bury him")
choice_results = PackedStringArray("Heavier pockets, lighter conscience.", "Useful, if grim.", "It costs you nothing but time.")
hp_deltas = PackedInt32Array(0, 0, 0)
gold_deltas = PackedInt32Array(90, 0, 0)
hp_percent_deltas = PackedInt32Array(0, 0, 0)
card_deltas = PackedInt32Array(0, 1, 0)
relic_grants = PackedInt32Array(0, 0, 1)
starts_fight = PackedInt32Array(0, 0, 0)
