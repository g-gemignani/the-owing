[gd_resource type="Resource" script_class="EventData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/event_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "gambler"
title = "The Gambler"
description = "A hooded figure shuffles a deck that is not cards."
choice_labels = PackedStringArray("Wager 60 gold", "Trade a card for a relic", "Refuse")
choice_results = PackedStringArray("You win more than you staked.", "The figure pockets your card and hands you something older.", "The figure shrugs.")
hp_deltas = PackedInt32Array(0, 0, 0)
gold_deltas = PackedInt32Array(60, 0, 0)
hp_percent_deltas = PackedInt32Array(0, 0, 0)
card_deltas = PackedInt32Array(0, -1, 0)
relic_grants = PackedInt32Array(0, 1, 0)
starts_fight = PackedInt32Array(0, 0, 0)
