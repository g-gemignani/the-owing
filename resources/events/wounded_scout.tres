[gd_resource type="Resource" script_class="EventData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/event_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "wounded_scout"
title = "A Wounded Scout"
description = "She will not make it out. She knows the way in."
choice_labels = PackedStringArray("Bind her wounds (-10 HP)", "Take her map (gain a card)", "Walk past")
choice_results = PackedStringArray("She points the way, then stops talking.", "The map is worth more than the promise.", "You hear nothing behind you.")
hp_deltas = PackedInt32Array(-10, 0, 0)
gold_deltas = PackedInt32Array(0, 0, 0)
hp_percent_deltas = PackedInt32Array(0, 0, 0)
card_deltas = PackedInt32Array(1, 1, 0)
relic_grants = PackedInt32Array(0, 0, 0)
starts_fight = PackedInt32Array(0, 0, 0)
