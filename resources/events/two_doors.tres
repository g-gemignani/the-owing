[gd_resource type="Resource" script_class="EventData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/event_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "two_doors"
title = "Two Doors"
description = "One is warm. One is not."
choice_labels = PackedStringArray("The warm door (fight an elite)", "The cold door (-15% HP)", "Neither — go back")
choice_results = PackedStringArray("Something was waiting.", "The cold gets into you, and you find a way through.", "You lose time, and nothing else.")
hp_deltas = PackedInt32Array(0, 0, 0)
gold_deltas = PackedInt32Array(0, 0, 0)
hp_percent_deltas = PackedInt32Array(0, -15, 0)
card_deltas = PackedInt32Array(0, 1, 0)
relic_grants = PackedInt32Array(0, 0, 0)
starts_fight = PackedInt32Array(1, 0, 0)
