[gd_resource type="Resource" script_class="EventData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/event_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "bloodied_altar"
title = "The Bloodied Altar"
description = "The channels are cut for a purpose."
choice_labels = PackedStringArray("Give freely (-20% HP)", "Give coin (-80 gold)", "Refuse")
choice_results = PackedStringArray("The stone drinks and answers.", "Coin works. It always does.", "Nothing happens. Yet.")
hp_deltas = PackedInt32Array(0, 0, 0)
gold_deltas = PackedInt32Array(0, -80, 0)
hp_percent_deltas = PackedInt32Array(-20, 0, 0)
card_deltas = PackedInt32Array(1, 1, 0)
relic_grants = PackedInt32Array(0, 0, 0)
starts_fight = PackedInt32Array(0, 0, 0)
