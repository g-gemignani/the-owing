[gd_resource type="Resource" script_class="EventData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/event_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "gambling_ghost"
title = "The Gambling Ghost"
description = "It has nothing left to lose and wants company."
choice_labels = PackedStringArray("Play for coin (+120 gold)", "Play for its trinket (-60 gold)", "Decline")
choice_results = PackedStringArray("You win. It laughs anyway.", "It hands over something cold.", "It keeps dealing to nobody.")
hp_deltas = PackedInt32Array(0, 0, 0)
gold_deltas = PackedInt32Array(120, -60, 0)
hp_percent_deltas = PackedInt32Array(0, 0, 0)
card_deltas = PackedInt32Array(0, 0, 0)
relic_grants = PackedInt32Array(0, 1, 0)
starts_fight = PackedInt32Array(0, 0, 0)
