[gd_resource type="Resource" script_class="EventData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/event_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "forgotten_library"
title = "The Forgotten Library"
description = "Most of it is ruined. Most."
choice_labels = PackedStringArray("Read carefully (gain a card)", "Take a page and run (+40 gold)", "Leave it be")
choice_results = PackedStringArray("One book is still legible.", "Paper sells.", "It keeps its secrets.")
hp_deltas = PackedInt32Array(0, 0, 0)
gold_deltas = PackedInt32Array(0, 40, 0)
hp_percent_deltas = PackedInt32Array(0, 0, 0)
card_deltas = PackedInt32Array(1, 0, 0)
relic_grants = PackedInt32Array(0, 0, 0)
starts_fight = PackedInt32Array(0, 0, 0)
