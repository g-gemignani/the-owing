[gd_resource type="Resource" script_class="EventData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/event_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "mirror_pool"
title = "The Mirror Pool"
description = "Your reflection is a moment behind you."
choice_labels = PackedStringArray("Reach in (-18% HP)", "Drink (heal 25%)", "Leave")
choice_results = PackedStringArray("You pull something out. It is yours now.", "Cold, and clean.", "The reflection waves.")
hp_deltas = PackedInt32Array(0, 0, 0)
gold_deltas = PackedInt32Array(0, 0, 0)
hp_percent_deltas = PackedInt32Array(-18, 25, 0)
card_deltas = PackedInt32Array(1, 0, 0)
relic_grants = PackedInt32Array(0, 0, 0)
starts_fight = PackedInt32Array(0, 0, 0)
