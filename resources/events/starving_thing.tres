[gd_resource type="Resource" script_class="EventData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/event_data.gd" id="1"]

[resource]
script = ExtResource("1")
id = "starving_thing"
title = "The Starving Thing"
description = "It is not hunting. It is begging."
choice_labels = PackedStringArray("Feed it (-10% HP)", "Kill it (fight an elite)", "Back away")
choice_results = PackedStringArray("It follows you a while, then helps.", "It barely fought.", "It watches you go.")
hp_deltas = PackedInt32Array(0, 0, 0)
gold_deltas = PackedInt32Array(0, 0, 0)
hp_percent_deltas = PackedInt32Array(-10, 0, 0)
card_deltas = PackedInt32Array(1, 0, 0)
relic_grants = PackedInt32Array(0, 0, 0)
starts_fight = PackedInt32Array(0, 1, 0)
